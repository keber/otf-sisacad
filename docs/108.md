### ✅ Task AB#108 - Create the `ProgramaFormativoService` service

**Objective:**
Implement the service layer for the `ProgramaFormativo` entity, covering the basic CRUD operations using a TDD approach (RED - GREEN - REFACTOR).

---

#### 🔁 TDD cycles performed:

| Cycle    | Description                                                                                                                                          |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| RED      | Unit tests were written for each operation: `guardar`, `listar`, `buscarPorId`, `actualizar`, `eliminar`.                                             |
| GREEN    | The methods in `ProgramaFormativoService` that use `ProgramaFormativoRepository` were implemented.                                                   |
| REFACTOR | `actualizarPrograma` was refactored to include ID-consistency validation and to throw the custom exception `ProgramaNoEncontradoException`.           |

---

#### 🧪 Tests implemented (`ProgramaFormativoServiceTest`)

* `debeGuardarProgramaFormativo`
* `debeListarTodosLosProgramas`
* `debeBuscarProgramaPorId`
* `debeActualizarProgramaExistente`
* `debeEliminarProgramaExistente`

---

#### ⚙️ Implementation (`ProgramaFormativoService`)

* `guardarPrograma(ProgramaFormativo programa)`
* `listarTodos()`
* `buscarPorId(Long id)`
* `actualizarPrograma(Long id, ProgramaFormativo actualizado)`
* `eliminarPrograma(Long id)`

---

#### 🧼 Refactoring performed

* Removed redundant code by using `orElseThrow` to avoid the explicit `if/else`.
* Added explicit validation to prevent updates with inconsistent IDs.
* Created a custom exception `ProgramaNoEncontradoException` to improve traceability and error semantics.

---

#### ✅ Relevant commits

```
fe8f608 test: add test to check ProgramaFormativoService operations save, findAll and findById [RED]
ff9597e fix: add ProgramaFormativoService with save, findAll and findById [GREEN]
db9b019 test: add test for ProgramaFormativoService eliminarPrograma method [RED]
de0eb34 fix: add eliminarPrograma method in ProgramaFormativoService [GREEN]
1e6c305 test: add test to check actualizarPrograma method in ProgramaFormativoService [RED]
85d73fc fix: add actualizarPrograma method in ProgramaFormativoService [GREEN]
b3c6027 refactor: improve actualizaPrograma with validations and specific exceptions [REFACTOR]
```

---
