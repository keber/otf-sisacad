# AB#110 Create the TrainingProgram DTO and Mapper

## Objective

Decouple the `TrainingProgram` entity from the representation exposed by the REST API by implementing a DTO (`TrainingProgramDto`) and an associated mapper (`TrainingProgramMapper`).

## Scope

* Implementation of the `TrainingProgramDto` DTO with the required fields.
* Implementation of the `TrainingProgramMapper` mapper with static methods:

  * `toDto(TrainingProgram entity)`
  * `toEntity(TrainingProgramDto dto)`
* TDD cycle:

  * RED: created the `TrainingProgramMapperTest` test class with conversion tests.
  * GREEN: implemented the mapper and the DTO.
  * REFACTOR: consolidated repeated logic and added a private constructor to the mapper to prevent instantiation, following the SonarQube recommendation.

## Associated commits

```
8c58f35 AB#110---Crear-DTO-y-Mapper-ProgramaFormativo (#44)
refactor: add private constructor to ProgramaFormativoMapper (Sonar fix)
```

## Final status

✅ Mapper and DTO implemented manually. ✅ Complete unit tests. ✅ SonarQube finding fixed.
