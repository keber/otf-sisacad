# AB#110 Create the ProgramaFormativo DTO and Mapper

## Objective

Decouple the `ProgramaFormativo` entity from the representation exposed by the REST API by implementing a DTO (`ProgramaFormativoDTO`) and an associated mapper (`ProgramaFormativoMapper`).

## Scope

* Implementation of the `ProgramaFormativoDTO` DTO with the required fields.
* Implementation of the `ProgramaFormativoMapper` mapper with static methods:

  * `toDTO(ProgramaFormativo entidad)`
  * `toEntity(ProgramaFormativoDTO dto)`
* TDD cycle:

  * RED: created the `ProgramaFormativoMapperTest` test class with conversion tests.
  * GREEN: implemented the mapper and the DTO.
  * REFACTOR: consolidated repeated logic and added a private constructor to the mapper to prevent instantiation, following the SonarQube recommendation.

## Associated commits

```
8c58f35 AB#110---Crear-DTO-y-Mapper-ProgramaFormativo (#44)
refactor: add private constructor to ProgramaFormativoMapper (Sonar fix)
```

## Final status

✅ Mapper and DTO implemented manually. ✅ Complete unit tests. ✅ SonarQube finding fixed.
